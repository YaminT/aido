[package]
name = "aido-backend"
description = "Models the sudo / sudo-rs / doas backend: capability matrix, snippet generation, and the install plan. Pure — it decides what to write, never writes it."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
serde.workspace = true
thiserror.workspace = true

[dev-dependencies]
serde_json.workspace = true

[lints]
workspace = true
