[package]
name = "aido-config"
description = "Layered configuration for aido and ido: precedence, origin tracking, narrowing-only lower layers, and path resolution. Pure — it is handed values, it never reads them."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
serde.workspace = true
thiserror.workspace = true
toml.workspace = true

[dev-dependencies]
serde_json.workspace = true

[lints]
workspace = true
